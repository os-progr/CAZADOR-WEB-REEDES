{
    "name": "el-cazador-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install-all": "npm install --prefix server && npm install --prefix client",
        "build-client": "npm run build --prefix client",
        "start": "npm start --prefix server",
        "build": "npm run install-all && npm run build-client && npx playwright install chromium"
    },
    "engines": {
        "node": "18.x"
    }
}